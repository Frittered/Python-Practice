#reusing code is fine if I was an overacheiver before, right?
print('Let\s add two numbers! Is this really nth day material?')
def two_numbers_one_function(one, two):
    return print(int(one) + int(two))
two_numbers_one_function(input('Number One '), input('Number Two '))
