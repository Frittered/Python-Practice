'''problem 1:Write a program which will find all such numbers 
which are divisible by 7 but are not a multiple of 5, between 2000 and 3200 (both included).
The numbers obtained should be printed in a comma-separated sequence on a single line.'''

def modulo_range(lower, upper):
  global r
  numbers = []
  for i in list(range(lower, upper)):
    if i %5 == 0:
      continue
    elif i % 7 == 0:
      numbers.append(i)
  return print(numbers)

m = modulo_range(2000, 3200)
m
