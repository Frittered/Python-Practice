user_input = input('Enter your text for counting.  ')

def sort_and_count(text):
    new_text = sorted(set(text.split(' ')))
    for i in new_text:
        print(f'{i}: {text.split(" ").count(i)}')

sort_and_count(user_input)
