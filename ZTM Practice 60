n = input('Enter the number of recursive additions')
# lambda adds 100 for every recursive operation as it subtracts 1 from n and approaches 0
f = lambda n: f(n-1)+100 if n > 0 else 0
result = f(int(n))
print(result)
