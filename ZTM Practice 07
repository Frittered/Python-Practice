def grid_quick(x,y):
    grid = []
    for i in range(int(x)):
        grid_append =[]
        for e in range(int(y)):
            grid_append.append(i*e)
        grid.append(grid_append)
    return print(grid)

grid_quick(3,5)
