li = ['a', 'b', 'c', 'c', 'a']
count_list = print(' ,'.join([''.join(tuples) for tuples in[*zip([f'{i}:' for i in set(li)],
                                              [str(li.count(i)) for i in set(li)]
                                              )]]))
