class rectangle:   
    def __init__(self, w, l):
        self.width = int(w)
        self.length = int(l)
    def calculate_area(self):
        area =self.width* self.length
        return print(f'area = {area}')
r = rectangle(3,5)
r.calculate_area()
